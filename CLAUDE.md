# Project Notes

## Playwright Setup
- Playwright is installed and configured with Python (not Node.js)
- Use `python3` to run Playwright scripts
- Scripts using Playwright should be written in Python
